# 屿航 · 下一阶段开发 Spec

> 版本：v1.0 · 日期：2026-08-28
> 状态：已评审待开工
> 项目性质：个人自用、本地优先、无需多用户负载

---

## 1. 背景与现状校准

「屿航」是一个本地优先的数字网址导航站：极简展示风 + 极光背景 + 毛玻璃卡片，多标签分类导航。当前代码进度为 **v11**。

### 1.1 已完成（现状校准，修正 README 过时描述）

| 能力 | 状态 | 说明 |
|------|------|------|
| 站点数据管线 | ✅ | `data/sites.tsv`(469条) → `scripts/build.mjs` 清洗 → `web/data/sites.js` |
| 主页面展示 | ✅ | 卡片网格、49 标签导航、搜索、详情弹窗 |
| 本地互动 | ✅ | 收藏、最常访问、随机、4套皮肤、为你推荐、点击统计 |
| **登录/注册（Supabase）** | ✅ | 邮箱注册/登录、谷歌、微软、忘记密码、修改密码、密码强度校验；`config.js` 已含真实配置 |
| **云同步** | ✅（部分） | 收藏、点击统计、个人网站在线同步；`scripts/supabase-schema.sql` 已含 RLS 表与统计函数 |
| 前端框架 | ⚠️ | `web/js/app.js` 为 **1895 行 / 89KB 单文件**，含 89 个顶层函数 + 大量 `var`，耦合度高 |

> **关键校准**：README 标注「登录/云同步待添加」已过时，代码已超前实现。需更新 README。

### 1.2 已知问题（本轮要处理）

- P1 `app.js` 单文件过大、全局变量泛滥、模块边界不清。
- P2 README 与代码现状不符。
- P3 站点数据维护依赖「手改 tsv + 跑脚本」，门槛高。
- P4 主页面偏桌面，移动端适配不足。
- P5 无障碍（键盘/焦点）支持缺失。

---

## 2. 目标

本轮（v6）目标围绕用户确认的三大方向：

1. **后台管理界面（纯本地，无后端）** —— 核心交付。
2. **技术底座重构** —— 抽公共模块、为后台铺路。
3. **纯前端体验打磨** —— 排在后台跑通之后。

### 2.1 非目标（明确不做）

- ❌ 不接通/扩展云同步（已实现部分保持不动，仅校准文档）。
- ❌ 不做云端后台、不做多用户鉴权后台。
- ❌ 不改 `app.js` 现有功能逻辑（重构只做解耦，不改变行为）。

---

## 3. 总体技术方案

### 3.1 后台管理形态：纯本地 + 导出下载

浏览器安全模型禁止直接写本地文件，因此采用 **「浏览器内读取 → 管理 → 导出下载」** 模式：

```
web/admin.html ──► 读取 web/data/sites.js（当前数据）
       │
       ▼
    表格编辑（增/删/改/搜索过滤/校验）
       │
       ├──► 导出下载 新版 sites.js（直接替换 web/data/sites.js）
       └──► 导出下载 sites.tsv（8 列，兼容 build.mjs 再生）
```

- 主页面 `index.html` **保持双击即用**，不依赖任何本地服务。
- 管理页生成文件后，用户拖拽替换 `web/data/sites.js` 即可（单步操作）。
- 清洗/生成逻辑复用 `build.mjs` 的同一套实现，保证输出一致。

### 3.2 模块复用

将 `scripts/build.mjs` 的清洗 + 站点生成逻辑抽为**平台无关的公共模块**：

- 浏览器：`web/admin.html` 通过 `import` 使用。
- 命令行：`build.mjs` 继续 require/import 使用，行为不变。

抽取候选模块：
- `normalize.tsv`（原始 8 列 → 清洗后条目）
- `dedupe`（同网址去重）
- `suspect-url`（可疑/内网地址标记，如 路由器/联通/Edge新标签页 📶）
- `classify-tags`（多标签智能分类）
- `serialize`（→ `sites.js` / `sites.tsv`）

---

## 4. 路线图（按依赖排序）

### 阶段 A —— 技术底座（先做，为后台铺路）
| # | 任务 | 产出 | 优先级 |
|---|------|------|--------|
| A1 | 抽 `build.mjs` 清洗/生成为公共模块 | 浏览器+CLI 共用模块 | 高 |
| A2 | 校准 README（登录/云同步已完成） | README.md | 高 |
| A3 | `app.js` 拆模块 + 全局变量收口 | 多 ES module + 明确 State | 低（第三批） |

> A1 必须先于 B 系列：管理页依赖公共清洗模块。

### 阶段 B —— 纯本地后台管理（核心交付）
| # | 任务 | 产出 | 优先级 |
|---|------|------|--------|
| B1 | 新建 `web/admin.html` 管理页骨架（独立页，不污染主页面） | admin.html | 高 |
| B2 | 站点列表 + 搜索过滤 + 增/删/改 | 管理页核心功能 | 高 |
| B3 | 导出新版 `sites.js` + `sites.tsv` | 下载功能 | 高 |
| B4 | 数据完整性校验（空网址/重复网址/缺简介 → 红黄旗） | 校验面板 | 中 |
| B5 | 一键「用本地新增/编辑数据重建 sites.js」 | 重建按钮（可选） | 中 |

### 阶段 C —— 纯前端体验打磨（后台跑通后）
| # | 任务 | 产出 | 优先级 |
|---|------|------|--------|
| C1 | 移动端响应式适配 | 侧栏/卡片/弹窗适配 | 中 |
| C2 | 卡片渲染性能优化 | 可测量改善 | 中 |
| C3 | 动效节奏统一（入场/扫光/弹窗） | 一致性 | 低 |
| C4 | 可访问性补充（键盘导航、弹窗焦点管理） | A11y | 中 |

### 阶段 D —— 后续（可选）
- 云同步多设备冲突处理、设置项 UI。
- 公开部署时的反滥用（当前不涉及）。

---

## 5. 第一批（本里程碑）交付清单与验收标准

### 5.1 交付物
1. `lib/`（或 `web/lib/`）公共清洗/生成模块，浏览器与 CLI 双端可用。
2. `web/admin.html` + `web/admin/style.css` + `web/admin/admin.js`。
3. `scripts/build.mjs` 改为复用公共模块（行为不变）。
4. `README.md` 校准至 v11 真实状态。

### 5.2 验收标准
- [ ] 运行 `node scripts/test-search.mjs` 原有测试**仍全部通过**（证明 build 重构未破坏逻辑）。
- [ ] `node scripts/build.mjs` 输出的 `sites.js` 与重构前**逐字节一致**（或仅允许无损差异）。
- [ ] 打开 `web/admin.html`（双击）能列出全部 469 条站点。
- [ ] 管理页可搜索过滤、可编辑单条简介/网址/标签、可新增、可删除。
- [ ] 管理页导出 `sites.js` 能被主页面 `index.html` 正常加载并展示。
- [ ] 管理页导出 `sites.tsv` 能通过 `build.mjs` 重新生成一致结果。
- [ ] README 的「待添加」列表移除已完成的登录/云同步。

---

## 6. 技术约束

- **不引入构建工具**：浏览器原生 ES Module（`<script type="module">`），避免破坏「双击即用」。
- **不改变现有功能行为**：重构只做解耦，不动 `app.js` 业务逻辑（阶段 A3 单独安排，不与后台并行，避免踩脚）。
- **主页面零依赖本地服务**：任何改动不得让 `index.html` 需要 `node`/服务器才能打开。
- 安全密钥纪律：`service_role` 等密钥绝不进前端（沿用现状）。

---

## 7. 风险与对策

| 风险 | 对策 |
|------|------|
| 抽公共模块改变 build 输出 | 验收标准强制「逐字节一致 + 回归测试通过」 |
| ES Module 在旧浏览器不兼容 | 现代浏览器均支持；如遇问题退化为单文件内联 |
| 拆 app.js 与后台相互踩脚 | 明确排期：后台(独立、不碰 app.js) → 后再拆 app.js |
| 管理页误改数据 | 本地导出制，原 `sites.tsv` 保留为回滚源 |

---

## 8. 开工顺序（第一批 Todo）

1. A1 抽 `build.mjs` 公共模块（先冻结当前 build 输出作比对基线）。
2. A3 校准 README。
3. B1 管理页骨架。
4. B2 列表/编辑核心。
5. B3 导出下载。
6. B5 数据校验。
7. 跑验收标准全量回归。

> 阶段 C（体验打磨）与阶段 A3（拆 app.js）在第一批完成后另行排期。

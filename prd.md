# 屿航（Yūháng）· 数字岛屿导航站 — 产品需求文档（PRD）

> **文档状态**：V1.0（需求梳理，未编码）
> **面向对象**：产品 / 设计（后续喂入 Stitch 做原型图）/ 前端开发
> **项目定位**：一个本地优先、个人主页式的网址导航站——「每一个网站都是一座数字小岛，这里是你的航行起点。」
> **技术边界**：纯前端、双击即用、零构建工具；数据本地化优先，登录后可云同步（Supabase）；PWA 可安装。

---

## 一、产品概述

### 1.1 一句话定位
为个人用户打造的「数字岛屿」导航中枢：把分散在各处的常用网站，收纳进一个极简、可个性化、可同步的本地优先导航主页。

### 1.2 核心价值
- **导航聚合**：4214+ 站点、21 个分类标签，一站式触达。
- **个人主页感**：不是冷冰冰的工具站，而是带头像、标语、数据、IP 角色的「个人岛屿主页」。
- **本地优先 + 轻量**：离线可用、双击打开，无沉重服务端依赖。
- **可个性化**：9 套主题 × 7 种强调色 + 整屏起始页，风格自由切换。
- **可同步**：登录后收藏 / 工作区 / 点击统计跨设备同步。

### 1.3 设计约束（对开发/原型的硬约束，Stitch 阶段不必过度纠结视觉，但结构须符合）
1. 移动端须全面适配（底部固定导航、卡片紧凑、分类浓缩为筛选下拉）。
2. 所有状态须有空状态 / 加载状态 / 错误降级文案与兜底。
3. 尊重系统「减少动态效果」设置；可访问性（焦点、方向键、Esc）。
4. 视觉风格（主题/配色）与页面结构/功能模块**解耦**——本 PRD 只定结构与功能。

---

## 二、目标用户画像

| 用户类型 | 特征 | 核心诉求 |
|---|---|---|
| **A. 工具收藏型** | 浏览器书签混乱、常用站点多 | 一站式收纳常用站，模糊搜索 + 标签筛选秒达 |
| **B. 效率工作者** | 需要工作区 / 待办 / 工具快速切入工作 | 工作区、待办、15 个本地小工具、番茄钟 |
| **C. 个性化表达型** | 喜欢美化的开始页 / 个人主页 | 9 主题 × 7 色、整屏起始页、IP 角色、数据展示 |
| **D. 多设备同步型** | 在电脑/手机/平板间切换 | 登录云同步收藏、工作区、点击统计 |
| **E. 站点站长/维护者** | 管理收录站点、查看反馈 | 后台管理页（增删改、批量操作、反馈、链接健康） |

**核心使用场景**：用户每次打开浏览器新标签 → 进入「岛屿主页」→ 搜索/筛选找站 / 点收藏直达 / 用工具 / 看起始页时钟与一言 / 登录同步。

---

## 三、页面列表与路由

> 当前为纯前端多页（`.html`）+ 主站内多「视图（View）」切换的模式。

| # | 页面/视图 | 入口 | 类型 |
|---|---|---|---|
| P0 | 主站首页（导航卡片视图） | `web/index.html` 默认视图 | 主视图 |
| P1 | 整屏起始页 | 悬浮球「🍋 整屏起始页」/ URL `#startpage` | 覆盖层/独立视图 |
| P2 | 收藏视图 | 底栏「收藏」/ 顶栏入口 | 主视图 |
| P3 | 最常访问视图 | 顶栏入口 | 主视图 |
| P4 | 今天去哪（随机） | 顶栏「🎲 今天去哪」 | 弹窗 |
| P5 | 为你推荐 | 顶栏「💡 为你推荐」 | 主视图/弹窗 |
| P6 | 搜索视图 + 联想下拉 | 底栏「搜索」/ 顶栏搜索框 `/` | 主视图 |
| P7 | 全网热搜视图 | 顶栏入口 | 主视图 |
| P8 | 全站排行视图（金/银/铜领奖台） | 底栏/顶栏「排行」 | 主视图 |
| P9 | 工具箱视图（15 工具） | 底栏「工具」/ 顶栏入口 | 主视图 |
| P10 | 工作区视图 | 顶栏/侧栏入口 | 主视图 |
| P11 | 个人数据/统计视图 | 顶栏「统计」 | 弹窗/视图 |
| P12 | 设置弹窗 | 悬浮球「设置」 | 弹窗 |
| P13 | 登录 / 注册弹窗 | 悬浮球「登录」 | 弹窗 |
| P14 | 待办清单页 | `web/todo.html` | 独立页面 |
| P15 | 屿咪 IP 图鉴页 | `web/ip.html` | 独立页面 |
| P16 | 后台管理页 | `web/admin.html` | 独立页面（站长） |

---

## 四、核心页面模块结构（逐页详述）

> 每个页面统一覆盖：**模块结构 / 关键词 / 交互要点 / 空状态 / 加载状态 / 移动端适配**。

---

### P0 · 主站首页（导航卡片视图）— 核心页

**角色**：产品的门面与主战场，承载分类筛选 + 卡片网格 + 顶部个人主页头。

**模块结构（自上而下）**
1. **个人主页式头部**：居中大头像（可拖拽回弹）+ 名字 + 标语 + 实时时钟 + 数据统计徽章（收录数 / 标签数 / 收藏数）。
2. **顶栏导航**：左侧搜索框（防抖 + 模糊）+ 引擎切换按钮；右侧收敛至右下悬浮球（登录 / 设置 / 随机）。
3. **分类筛选栏**：21 个标签 chip，左侧多选叠加筛选（当前选中高亮 + 计数）。
4. **卡片网格**：卡片 = 图标（真实 favicon，失败降级首字头像）+ 主标题 + 单一标签；支持卡片大小三档（小/中/大）；默认按名称字母排序（中文按拼音）。
5. **悬浮球（右下角）**：登录 / 设置 / 🎲 随机 / 🍋 整屏起始页 / ⬆ 回顶。
6. **移动端固定底部导航**：首页 / 搜索 / 收藏 / 工具 / 我的快捷栏（高亮跟随当前视图）。
7. **页脚**：免责声明（短）+ 官方 QQ 群入口。

**关键词**：极简展示 / 毛玻璃卡片 / 极光背景 / 多标签分类 / 字母排序 / 拖拽头像 / 视差滚动 / 卡片飞出动画。

**交互要点**
- 弹簧缓动入场 + 标题字母逐字弹簧 + 滚动联动视差（内容下沉/缩放/淡出）。
- 筛选/搜索被移除的卡片从原位置飞出（FLIP），不瞬间消失。
- 点击卡片 → 卡片上浮放大淡出（起飞反馈）→ 新标签页打开对应网址。
- 卡片详情弹窗：去重后的 name/fullName + 简介 + 访问量 + 收藏/打开/复制链接按钮 + 需加速器标注 ⚡。
- 键盘：`/` 聚焦搜索、`?` 快捷键说明、`Esc` 关弹窗、方向键在卡片间移动焦点。

**空状态**
- 某分类无站点：显示「该岛屿暂无站点 🏝️」插画占位 + 「换一个分类」引导 + 一键回到「其他」分类。
- 全局搜索无结果：显示「未找到对应岛屿」+ 展示站内搜索热词 + 「换个关键词」+ 直达多引擎搜索该词。

**加载状态**
- 骨架屏（卡片网格占位条）+ 顶部品牌加载动画（Logo 浮动 + 进度条），资源就绪后淡出衔接开场动画。
- 尊重「减少动态效果」时禁用视差/粒子/脉冲环等。

**移动端适配**
- 一排 **2 个卡片**（紧凑型）；分类栏浓缩为吸顶「筛选」下拉按钮（显示当前分类，点开列全部）；头像/时钟区域缩小；悬浮球改为底部导航常驻。

---

### P1 · 整屏起始页（青柠风格 · 起始页）

**角色**：主站之上的「起始屏幕」，借鉴起始页概念但融入屿航视觉体系，可独立进入。

**模块结构（自上而下）**
1. **大时钟 + 实时日期 + 时段问候**（「早安 / 午安 / 晚安」）。
2. **逐字弹簧「每日一言」**（点击换一句，随机诗词/名言）。
3. **多引擎搜索条**：自建同款下拉，与顶栏菜单双向同步（单一真相来源）；输入即联想（站内实时匹配 + 网址识别「直达」）。
4. **分类直达 chips**：取自站点标签，点击切主站对应分类并回到导航。
5. **只读投影区**：⭐ 收藏 / 🔥 最常访问 网站列表。
6. **快捷链接区**：默认 12 个取自站内真实收录 + 增删 + HTML5 拖拽排序 + 真实 favicon 多源回退。
7. **壁纸切换**：极光 / 海蓝 / 必应每日壁纸 / 暗夜（离线自动回落极光，不报错）。
8. 背景层：漂浮极光光斑 + 闪烁星光 + 主题柔光叠层。

**关键词**：起始页 / 大时钟 / 每日一言 / 多引擎搜索 / 输入即联想 / 快捷链接拖拽 / 壁纸切换 / 离线可用。

**交互要点**
- 与「个人主页式起始页」属同一套视觉语言，悬浮球可切换。
- 快捷链接 HTML5 拖拽排序，状态持久化（localStorage `nav_startpage_v1`）。
- 壁纸离线自动回落，不阻断渲染。

**空状态**
- 收藏 / 最常访问投影区为空：显示「开始浏览岛屿后，这里会出现你最爱的站点 ⭐」引导。
- 快捷链接为空：默认填充 12 个，允许清空 → 显示「拖拽添加你的常用站点」+ 一个「恢复默认」按钮。

**加载状态**
- 时钟/日期优先渲染（骨架极简）；一言与快捷链接后到，带淡入。

**移动端适配**
- 大时钟缩小、分段堆叠（时间 / 日期 / 问候纵向）；分类 chips 可横向滑动；快捷链接两列网格 + 移动端拖拽句柄。

---

### P2 · 收藏视图

**角色**：用户星标站点的专属岛屿，登录后可云同步，游客可本地收藏。

**模块结构**
1. 标题「⭐ 我的收藏」+ 收藏数量。
2. 卡片网格（同主站卡片样式，可筛选）。
3. 操作栏：一键全部打开（超 8 个先确认）+ 导出为书签（HTML，可导入任意浏览器）。

**关键词**：星标 / 一键打开 / 书签导出 / 云同步。

**交互要点**：超过 8 个「全部打开」需二次确认弹窗防误触；导出书签生成 HTML 文件下载。

**空状态**：「还未收藏任何岛屿，去主站点亮 ⭐ 吧」+ 一键跳回主站。

**加载状态**：登录态先显示本地收藏（即时），云端合并后增量刷新。

**移动端适配**：一排 2 卡；操作栏置卡片上方。

---

### P3 · 最常访问视图

**角色**：基于点击统计的「你的高频岛屿 Top」，本地 + 云端聚合。

**模块结构**
1. 标题「🔥 最常访问」+ 统计周期说明。
2. Top6 高亮（大卡/数字排名）+ 其后平铺列表。
3. 重置统计入口（谨慎：二次确认）。

**关键词**：点击统计 / Top 榜单 / 排名数字 / 重置确认。

**空状态**：「开始访问站点后，这里会记录你的足迹 🔥」+ 跳主站。

**加载状态**：统计数字滚动计数动画（首次进入）。

**移动端适配**：Top6 改为单列大卡 + 数字徽章。

---

### P4 · 今天去哪（随机）弹窗

**角色**：探索性入口，随机推荐一个站点。

**模块结构**
1. 随机选出的站点卡片（大图 + 简介）。
2. 两个按钮：「打开」/「换一个」。

**关键词**：随机 / 探索 / 二次确认 / 不自动跳转。

**交互要点**：不会直接跳转，弹窗询问用户；「换一个」再随机一次（带动画）。

**空状态**：站点库为空时显示「岛屿地图暂空」+ 跳主站。

**移动端适配**：弹窗全宽，按钮全宽。

---

### P5 · 为你推荐

**角色**：基于用户行为（访问/收藏）的个性化推荐。

**模块结构**
1. 标题「💡 为你推荐」+ 推荐逻辑一句话。
2. 推荐卡片网格。
3. 「不感兴趣 / 反馈」单条操作（减少该类推荐）。

**关键词**：个性化 / 推荐 / 反馈调节。

**空状态**：「多逛逛岛屿，我会更懂你 💡」+ 推荐最热站点兜底。

**移动端适配**：两列卡片。

---

### P6 · 搜索视图 + 联想下拉

**角色**：站内搜索中枢，模糊容错 + 多引擎直达。

**模块结构**
1. 大搜索框（自动聚焦）+ 引擎切换（站内/百度/谷歌/必应/B站/知乎/GitHub）。
2. 联想下拉：实时匹配站名（点击直达）+ 识别网址时「直达」+ 底部一键站内搜索该词。
3. 热搜/站内搜索热词快捷条（搜索框聚焦时展示）。
4. 搜索结果卡片网格（精确命中置顶）。

**关键词**：模糊匹配 / 错别字容错（chagpt→ChatGPT）/ 多引擎 / 输入即联想 / 精确命中置顶。

**交互要点**：防抖；联想下拉用方向键选择、Enter 直达。

**空状态**：无结果 → 同 P0 空态（热词 + 换词 + 多引擎直达）。

**加载状态**：联想下拉骨架行；结果区骨架卡。

**移动端适配**：搜索框满宽吸顶；引擎切换改为横向滚动 chip；联想下拉全宽。

---

### P7 · 全网热搜视图

**角色**：聚合多平台热搜，多源接口自动回退。

**模块结构**
1. 平台 Tab（微博 / 知乎 / 百度 / 抖音 / B站 / 头条）。
2. 信息条：平台 / 更新时间 / 条数。
3. 榜单列表（每条：排名 + 标题 + 热度）。
4. 本地过滤框（榜单内快速筛选）。

**关键词**：多源聚合 / 自动回退 / 信息条 / 榜单筛选。

**交互要点**：任一源可用即出榜；全部不可用 → **不报错文案、不自动跳转**，仅给各平台直达链接由用户自点。

**空状态**：全部源不可用时 → 「暂无法获取热搜 📡」+ 各平台直达链接列表（无红色报错）。

**加载状态**：榜单行骨架 + 计时超时回退。

**移动端适配**：平台 Tab 横向滑动；榜单单列。

---

### P8 · 全站排行视图

**角色**：全站收藏榜 / 点击榜，登录/分享统计开启后参与。

**模块结构**
1. Tab 切换：收藏榜 / 点击榜。
2. 前三名金/银/铜领奖台（大卡 + 名次徽章）。
3. 第四名起平铺列表（名次 + 站点 + 数值）。
4. 分享统计开关提示（关闭则不参与）。

**关键词**：领奖台 / 金银铜 / 收藏榜 / 点击榜 / 排名。

**交互要点**：领奖台视觉突出 Top3。

**空状态**：无数据 → 「大家都在探索，还没有排行 🏆」+ 跳主站。

**加载状态**：领奖台骨架 + 数字滚动计数。

**移动端适配**：领奖台缩小为横向 3 卡；列表单列。

---

### P9 · 工具箱视图（15 个本地小工具）

**角色**：纯本地实用工具集合，无需网络。

**模块结构**
1. 工具网格（图标 + 名称），分组：
   - 编码/文本：随机密码、Base64、时间戳、JSON、字数统计、UUID、颜色转换、URL 编解码、正则测试、单位换算。
   - 计算/可视化：计算器、函数图像。
   - 生活/效率：番茄钟、二维码、IP 查询。
   - 快捷查询：快递 / 航班 / 汇率。
2. 点击展开工具面板（就地或弹窗）。

**关键词**：本地工具 / 15 个 / 番茄钟 / 二维码 / 函数图像 / 无网络依赖。

**交互要点**：纯前端计算，结果可复制；番茄钟含倒计时 + 完成提示。

**空状态**：单个工具无输入时显示示例/说明占位（如计算器显示 `0`、二维码显示「请输入内容」）。

**加载状态**：工具面板即刻显示（本地计算无加载）；联网型（IP/快递）显示加载条 + 失败回退提示。

**移动端适配**：工具网格两列；番茄钟计时全屏化。

---

### P10 · 工作区视图

**角色**：用户自建的工作分组（如「今日项目」「学习资料」），拖拽排序。

**模块结构**
1. 工作区列表（可拖拽调整顺序）。
2. 每个工作区：名称 + 内含站点列表。
3. 新增 / 编辑 / 删除工作区。
4. 「打开全部」入口（该工作区内）。

**关键词**：分组 / 拖拽排序 / 自定义 / 云同步。

**交互要点**：HTML5 拖拽排序；登录后云同步。

**空状态**：无工作区 → 「创建你的第一个工作区 📂」+ 示例引导。

**加载状态**：云端工作区加载骨架。

**移动端适配**：工作区单列可折叠手风琴。

---

### P11 · 个人数据/统计视图

**角色**：我的使用数据展示。

**模块结构**
1. 数据卡片：收藏 / 点击 / 我的网站 / 工作区 计数。
2. 最常访问 Top6。
3. 我的标签分布（可视化）。

**关键词**：数据统计 / Top6 / 标签分布 / 纯本地。

**空状态**：新账号 → 各项为 0 + 「开始浏览即可积累数据」引导。

**移动端适配**：数据卡两列网格。

---

### P12 · 设置弹窗

**角色**：全局偏好设置。

**模块结构**
1. **主题**：9 套风格（终端 / 杂志 / 粗野主义 / 赛博霓虹 / 像素 / 瑞士极简 / 玻璃拟态 / 手帐 / 3D空间）。
2. **强调色**：7 种颜色选择器。
3. **卡片**：大小三档 / 图标模式（网站图标 / 首字）。
4. **分享统计**：开关（影响排行榜与云同步贡献）。
5. **数据**：导出 / 导入本地数据（JSON）；浏览器书签导入；收藏导出为书签（HTML）。
6. **链接健康**：导入链接报告入口（提示）。
7. 自动记忆所有设置（localStorage）。

**关键词**：9 主题 × 7 色 / 卡片大小 / 数据备份 / 书签导入导出 / 自动记忆。

**交互要点**：设置即时生效预览；导出/导入带 Toast 反馈。

**空状态**：书签导入无文件时显示「选择 HTML 书签文件」。

**移动端适配**：滚动单列；颜色选择器横向。

---

### P13 · 登录 / 注册弹窗

**角色**：账号体系入口（Supabase）。

**模块结构**
1. **登录**：邮箱 + 密码；忘记密码入口；修改密码入口。
2. **注册说明**：注册需管理员开通 → 一键复制「用户名/密码」申请内容 → 发邮件至站长邮箱（`jubei516206@163.com`）。
3. 登录后解锁：个人网站 / 收藏 / 最常 / 工作区 / 排行榜 / 为你推荐。

**关键词**：邮箱登录 / 忘记密码 / 管理员开通 / 一键复制申请 / 解锁功能。

**交互要点**：申请注册用 mailto 打开邮箱客户端；登录失败友好提示；云同步状态展示。

**空状态**：未登录态各功能显示「登录解锁」引导层。

**加载状态**：登录按钮 loading 态。

**移动端适配**：表单单列全宽。

---

### P14 · 待办清单页（`web/todo.html`）

**角色**：独立待办，屿咪伴随。

**模块结构**
1. 新增待办（标题 + 优先级 高/中/低 + 分类标签）。
2. 待办列表（增删 / 完成勾选 / 优先级色标）。
3. 多维筛选（优先级 / 分类 / 完成状态）。
4. 进度统计（完成率）。
5. 屿咪 IP 伴随（按任务状态切换开心/惊讶/打盹表情）。
6. 完成触发庆祝动画。
7. localStorage 持久化 + 深色模式自适应。

**关键词**：待办 / 优先级 / 分类 / 进度 / 屿咪 / 庆祝动画 / 持久化。

**交互要点**：完成待办时屿咪表情变化 + 庆祝动效；尊重减少动态效果。

**空状态**：「还没有待办，添加第一个任务吧 📋」+ 屿咪打盹表情 + 示例模板。

**移动端适配**：表单与列表单列；筛选 chip 横向滚动。

---

### P15 · 屿咪 IP 图鉴页（`web/ip.html`）

**角色**：吉祥物展示 + 互动。

**模块结构**
1. 角色设定介绍（屿咪 · 数字岛屿领航员：紫蓝极光毛色 / 星尖尾巴 / 圆润大眼）。
2. 表情图鉴（开心/惊讶/打盹等）。
3. 互动点击触发庆祝动画。
4. 复用 `ip.css` 独立角色样式。

**关键词**：IP 角色 / 图鉴 / 互动 / 庆祝动画。

**空状态**：无（展示型页面）。

**移动端适配**：图鉴单列大图。

---

### P16 · 后台管理页（`web/admin.html`，站长）

**角色**：站点数据管理与用户反馈。

**模块结构**
1. **站点列表**：搜索过滤（模糊增强）+ 分页（10/20/50 + 页码窗）+ 排序（名称/大类）。
2. **CRUD**：双击快速编辑 / 新增 / 删除（可撤销）。
3. **批量操作**：勾选多行 / 表头全选 → 批量删除 / 批量改标签 / 批量改大类。
4. **校验面板**：标出空网址 / 重复网址 / 缺简介。
5. **导出**：💾 保存到 `sites.js`（File System Access API，HTTPS/localhost）；导出 `sites.tsv`（Excel 二次编辑）。
6. **反馈记录**：📣 查看所有用户反馈（站长邮箱 `xvwang99@126.com` 登录后可见），可删除。
7. **导入链接报告**：`linkcheck-report.json` 一键标记失效链接。
8. 所有操作 Toast 提示。

**关键词**：后台 / 增删改 / 批量操作 / 撤销 / 校验 / 反馈管理 / 链接健康。

**交互要点**：保存优先用文件系统 API 直接写入，失败降级为下载文件 + 提示用本地服务打开。

**空状态**：反馈为空 →「暂无用户反馈」；站点为空 →「还没有收录站点，去添加吧」。

**加载状态**：列表分页加载骨架。

**移动端适配**：列表自动转为 3 列迷你卡片；批量操作改为底部操作栏。

---

## 五、跨页面公共模块

| 模块 | 说明 |
|---|---|
| **加载界面** | 品牌 Logo 浮动 + 进度条，`is-loading` 期间暂停开场动画，资源就绪后淡出衔接。 |
| **悬浮球（右下角）** | 登录 / 设置 / 🎲 随机 / 🍋 整屏起始页 / ⬆ 回顶；移动端收敛进底部导航。 |
| **底部固定导航（移动端）** | 首页 / 搜索 / 收藏 / 工具 / 我的快捷栏，高亮跟随当前视图。 |
| **顶栏搜索** | 站内模糊 + 多引擎切换 + 输入即联想（与 P1/P6 共享单一真相来源）。 |
| **免责声明弹窗** | 进入时仅弹一次，可「不再提醒」；页脚常驻短免责。 |
| **Toast 提示** | 全局轻量反馈（操作成功/失败）。 |
| **快捷键说明弹窗** | `?` 触发：`/` 搜索、`Esc` 关闭、方向键焦点、`?` 帮助。 |
| **最近访问** | 本地记录最近 30 个站点，侧栏「🕘 最近」一键找回（游客可用）。 |
| **一键回顶** | 滚动后右下角平滑回顶。 |
| **网站反馈** | 官方站点一键提交 → 直接 mailto 站长邮箱。 |
| **漫游模式** | 悬浮球开启，每 4 秒随机打开一个网站，再点停止。 |

---

## 六、移动端适配统一规范

1. **导航**：顶栏精简，功能收敛至右下悬浮球 + 底部固定导航（5 项）。
2. **卡片**：主站一排 2 个紧凑卡片；排行 Top3 横向 3 卡；列表/工具两列网格。
3. **筛选**：分类栏浓缩为吸顶「筛选」下拉按钮。
4. **弹窗**：全宽底弹或全屏，按钮全宽。
5. **横滑**：平台 Tab / 筛选 chip / 颜色选择器支持横向滚动。
6. **手势**：拖拽排序需移动端句柄；卡片右滑可「收藏/详情」。
7. **触控**：按钮最小触控区 44×44；避免依赖 hover（hover 态改为 tap 态）。
8. **PWA**：manifest + 主题色 + SVG 图标，可「添加到主屏幕」；离线缓存应用外壳。

---

## 七、状态体系总览（空 / 加载 / 错误兜底）

| 状态 | 全局规范 |
|---|---|
| **空状态** | 统一插画/emoji + 一句话说明 + 一个行动入口（回主站/创建/探索）；热搜与排行空态**不显示红色报错**，只给兜底直达链接。 |
| **加载状态** | 骨架屏（卡片/列表行）+ 品牌进度条；本地计算类无加载；联网类（热搜/IP/快递）超时自动回退。 |
| **错误/降级** | 接口不可用 → 不报错文案、不自动跳转，给手动直达入口；离线 → PWA 缓存 + 网络优先；单元素空值保护防止整页崩溃。 |
| **动态效果** | 全部尊重系统「减少动态效果」设置，可整站关闭视差/粒子/脉冲/飞出/庆祝动画。 |

---

## 八、数据与同步模型（产品层描述）

- **站点主数据**：`data/sites.tsv`（4214 条，21 分类）→ 构建为 `web/data/sites.js`，PWA 走「网络优先」，后台修改后刷新即生效。
- **本地数据**：收藏 / 点击统计 / 工作区 / 个人网站 / 设置 → localStorage，可 JSON 导出/导入；书签双向互通（HTML 导入 / 收藏导出 HTML）。
- **云同步**（登录后，Supabase + RLS 行级权限）：收藏 / 点击统计 / 个人网站随账号跨设备；「分享统计」开关关闭则不参与排行榜。
- **排行**：只读聚合，不暴露个人明细。
- **反馈**：普通用户提交自己的反馈；站长专属查看/删除全部反馈。

---

## 九、验收要点（供后续开发对照）

1. 9 主题 × 7 色可自由切换，浅色基底不变，风格与配色独立记忆。
2. 站内模糊搜索含错别字容错（chagpt→ChatGPT、bilibili→哔哩哔哩），精确命中置顶。
3. 移动端主站一排 2 卡、底部 5 项导航、分类筛选下拉。
4. 登录前后功能分层清晰，未登录态有「登录解锁」引导。
5. 所有页面无单点空值崩溃；热搜/排行空态不显红报错。
6. 尊重「减少动态效果」，PWA 可安装且离线可用。
7. 后台管理页支持增删改/批量/撤销/校验/反馈/链接报告导入。

---

*本 PRD 聚焦功能模块与页面结构，视觉风格留待后续交由 Stitch 出原型图定稿。*
